# HotelWorks 프로덕션 환경 변수 템플릿
# 이 파일을 .env로 복사하고 실제 값으로 변경하세요
# cp env.production.template .env

# 데이터베이스 설정
DB_HOST=localhost
DB_PORT=3306
DB_USER=hotelworks_user
DB_PASSWORD=your_secure_password_here_change_this
DB_NAME=hotelworks

# 서버 설정
PORT=3001
NODE_ENV=production

# 도메인 설정 (실제 도메인 또는 IP로 변경)
SERVER_URL=https://your-domain.com

# WebSocket URL (프론트엔드에서 사용)
# 도메인이 있는 경우:
# VITE_WS_SERVER_URL=https://your-domain.com
# IP만 있는 경우:
# VITE_WS_SERVER_URL=http://your-server-ip:3001

# 보안 설정 (선택)
# SESSION_SECRET=your_random_secret_key_here
# JWT_SECRET=your_jwt_secret_here

# 로깅 설정 (선택)
# LOG_LEVEL=info
# LOG_FILE=/var/log/hotelworks/app.log

